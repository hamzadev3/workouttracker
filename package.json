{
  "name": "workout-tracker",
  "private": true,
  "scripts": {
    "dev:api": "nodemon apps/api/src/server.js",
    "dev:web": "vite --host --port 5173 --config apps/web/vite.config.js",
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "test": "npm -w apps/api test && npm -w apps/web test"
  },
  "devDependencies": {
    "concurrently": "^8",
    "nodemon": "^3"
  }
}